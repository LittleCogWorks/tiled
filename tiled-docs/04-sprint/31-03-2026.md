




Controller:

UI needs to fit mobile device. 

- Player (P) uses browser to connect to host.
- P presented with connection screen:
	- Host (prefilled)
	- Connect button -> connects player to lobby and presents them the 'user' screen
- User screen: P enters name and select avatar (from a drop down or icons in a grid)
	- Name test field
	- Avatar icons in grid. texture buttons.
	- Join button -> updates P information on lobby badge. Whilst waiting for game to start, this screen remains and P can update their choices. Button changes from Join to update.
- Game start: Needs to show whose turn it is. needs to provide buttons acting as tiles - currently as in the debug UI in use now. 
  Need a guess button. When pressed indicates someone is guessing on the main screen, whilst providing a text input for the player. 
  Need the continue button to navigate any onscreen actions, like current transition though that does have a timer (like the manual speeed up!)



**Now**

- ~~Settings rearrangement.~~
- ~~Remove fuzzy option from settings UI.~~
- ~~Button click sounds and pressed/focus polish.~~
- Confirm/dialogue replacement.
- Single-player input modal polish.
- Controller mobile layout baseline.
- Enum migration for game_mode and game_type.

**Next**

- Controller connection and player setup screens.
- Controller in-game screen with turn states.
- Guess flow integration on controller and host display.
- Manual continue support on controller for overlays.

**Later**

- Tile cover style packs and colour variants.
- More detailed results on controller.
- Tone/style variants for UI messaging and overlays.



1. Add a single Master Volume slider that scales both music and sfx together, while preserving per-channel sliders.
2. Split options into tabs/pages (Audio, Internet, Gameplay, Accessibility) like the Drawful-style references you shared.
3. Add short helper text under the currently focused setting so controller navigation feels clearer.



game_init.gd
```
"game_mode": "multi"
```

qna.gd
```
if GameManager.game.game_mode == "multi":
```

game.gd

```
var game_mode: String = ""      # e.g., "single", "multi", "pass_and_play"
var game_type: String = ""      # e.g., "classic", "timed", "challenge"
```

Change these to take enum to avoid potential mistyping. Maybe not ENUMS but dictionary like in `app.js`
```
const ControllerState = {

    DISCONNECTED: "disconnected",

    CONNECTED_UNJOINED: "connected_unjoined",

    PROFILE_EDIT: "profile_edit",

    LOBBY_READY: "lobby_ready",

    WAITING_TURN: "waiting_turn",

    ACTIVE_TURN: "active_turn",

    VOTING: "voting",

    ROUND_RESULT: "round_result",

    GAME_OVER: "game_over",

};
```





### Controller:
- when connected, hide the connected panel.
- separate vote controls, hide until a vote situation.
- guess: think this needs to be a two step process. press guess so the main game can update the display, maybe some witty message, the press also displays the text input on the controller. Otherwise its trying to pick up when the player enter the text field, the game could update like a messaging app with "N is typing...". Faffy and probs not needed! Challenge though?1
- clear text field after enter
- phone locked, i got disconnected, the game dint notice and i couldn't reconnect

---

the reconnect just seems to connect to the lobby. Got to think how to handle disconnections.  If not reconnecting back into the game at the plaace htey left, then maybe once off if not enough players return to lobby otherwise play on.

Just tested: if the player ends up back a tthe connect state, they press connect then join but this does not return them to the game, i beleive it leaves them in the lobby state.

I also need to fix the last person standing, as the answer modal on the main screen is still loading after the overlay finishes. 